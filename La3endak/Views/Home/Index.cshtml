@model Tuple<List<La3endak.Models.Subject>, List<La3endak.Models.UserAccount>, List<La3endak.Models.Testimonial>, List<La3endak.Models.EducationLevel>>
@{
    ViewData["Title"] = "Book Academic Session";
    Layout = "~/Views/Shared/_Layout.cshtml";
    var userLocation = ViewBag.UserLocation as string;
}
 <!-- Welcome Intro Animation -->
    <div class="le-welcome-intro">
        <div class="le-intro-content">
            <div class="le-intro-text">
                <span class="le-welcome-letters">L</span>
                <span class="le-welcome-letters">a</span>
                <span class="le-welcome-letters">3</span>
                <span class="le-welcome-letters">e</span>
                <span class="le-welcome-letters">n</span>
                <span class="le-welcome-letters">d</span>
                <span class="le-welcome-letters">a</span>
                <span class="le-welcome-letters">k</span>
            </div>
            <div class="le-intro-subtitle">Empowering Academic Excellence</div>
            <div class="le-intro-loader"></div>
        </div>
    </div>

<!-- Hero Section -->
<section class="le-hero-sections">
    <div class="le-hero-container">
        <!-- Animated Background Elements -->
        <div class="le-particles"></div>
        
        <!-- Main Content -->
        <div class="le-hero-content">
            <!-- Text Content -->
            <div class="le-text-content">
                <div class="le-title-group">
                    <h1 class="le-main-title">
                        <span class="le-gradient-text">Empowering</span> Future Scholars
                    </h1>
                    <div class="le-title-underline"></div>
                </div>
                
                <p class="le-subtitle">
                    Premium Academic Guidance & Mentorship Programs
                </p>

                <!-- Welcome Badge -->
                <div class="le-welcome-card">
                    <i class="bi bi-stars"></i>
                    <div class="le-welcome-text">
                        <span class="le-welcome-title">Welcome to La3endak</span>
                        <span class="le-welcome-sub">Your Academic Journey Starts Here</span>
                    </div>
                    <div class="le-underline-animation"></div>
                </div>

                <!-- Stats Grid -->
                      <div class="le-stats-grid">
                    <div class="le-stat-card">
                        <div class="le-stat-number" data-count="98">
            @ViewBag.NumberOfTeacher </div>
                        <div class="le-stat-label">Teacher Works</div>
                    </div>
                    <div class="le-stat-card">
                        <div class="le-stat-number" data-count="150"> @ViewBag.NumberOfUser</div>
                        <div class="le-stat-label">Student </div>
                    </div>
                    <div class="le-stat-card">
                        <div class="le-stat-number" data-count="500">@ViewBag.NumberOfSubject</div>
                        <div class="le-stat-label">Subject Available</div>
                    </div>
                </div>

                <!-- CTA Buttons -->
                <div class="le-cta-group">
                    <a href="#programs" class="le-main-cta">
                        <span>Explore Programs</span>
                        <div class="le-cta-hover"></div>
                        <i class="bi bi-arrow-up-right"></i>
                    </a>
                    <a href="#contact" class="le-secondary-cta">
                        <span>Consult Advisor</span>
                        <div class="le-cta-hover"></div>
                        <i class="bi bi-chat-dots"></i>
                    </a>
                </div>
            </div>

            <!-- Visual Section -->
            <div class="le-visual-content">
                <div class="le-image-container">
                    <img src="images/academic-hero.png" alt="Academic Community" class="le-main-image">
                    <div class="le-live-badge">
                        <div class="le-pulse-dot"></div>
                        Live Sessions Available
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<style>
:root {
    --accent-gold: #FFD700;
    --dark-blue: #2A5C82;
    --deep-navy: #0A192F;
    --pure-white: #FFFFFF;
    --gradient-primary: linear-gradient(135deg, var(--dark-blue) 0%, var(--deep-navy) 100%);
    --gradient-accent: linear-gradient(45deg, var(--accent-gold) 0%, #FFB300 100%);
}

        /* Welcome Intro Styles */
        .le-welcome-intro {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: var(--gradient-primary);
            z-index: 9999;
            display: flex;
            justify-content: center;
            align-items: center;
            animation: introFade 3s forwards;
        }

        .le-intro-content {
            text-align: center;
            transform: translateY(-20%);
        }

        .le-intro-text {
            font-family: 'Playfair Display', serif;
            font-size: 4rem;
            margin-bottom: 1rem;
            position: relative;
        }

        .le-welcome-letters {
            display: inline-block;
            color: var(--accent-gold);
            opacity: 0;
            transform: translateY(20px);
            animation: letterEntrance 0.8s forwards;
        }

        @@keyframes letterEntrance {
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .le-welcome-letters:nth-child(1) { animation-delay: 0.1s; }
        .le-welcome-letters:nth-child(2) { animation-delay: 0.2s; }
        .le-welcome-letters:nth-child(3) { animation-delay: 0.3s; }
        .le-welcome-letters:nth-child(4) { animation-delay: 0.4s; }
        .le-welcome-letters:nth-child(5) { animation-delay: 0.5s; }
        .le-welcome-letters:nth-child(6) { animation-delay: 0.6s; }
        .le-welcome-letters:nth-child(7) { animation-delay: 0.7s; }
        .le-welcome-letters:nth-child(8) { animation-delay: 0.8s; }

        .le-intro-subtitle {
            color: rgba(255, 255, 255, 0.9);
            font-size: 1.5rem;
            opacity: 0;
            animation: subtitleEntrance 1s forwards 1.5s;
        }

        @@keyframes subtitleEntrance {
            from {
                opacity: 0;
                transform: translateY(10px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .le-intro-loader {
            width: 40px;
            height: 40px;
            border: 3px solid rgba(255, 255, 255, 0.2);
            border-top-color: var(--accent-gold);
            border-radius: 50%;
            margin: 2rem auto;
            opacity: 0;
            animation: 
                loaderSpin 1s linear infinite,
                loaderEntrance 0.5s forwards 2s;
        }

        @@keyframes loaderSpin {
            to { transform: rotate(360deg); }
        }

        @@keyframes loaderEntrance {
            from { opacity: 0; transform: scale(0.5); }
            to { opacity: 1; transform: scale(1); }
        }

        @@keyframes introFade {
            0% { opacity: 1; visibility: visible; }
            80% { opacity: 1; }
            100% { opacity: 0; visibility: hidden; }
        }


/* Base Styles */
.le-hero-sections {
    position: relative;
    min-height: 100vh;
    background: var(--gradient-primary);
    overflow: hidden;
    padding: 2rem;
}

.le-hero-container {
    max-width: 1440px;
    margin: 0 auto;
    position: relative;
    z-index: 2;
}

/* Particles Animation */
.le-particles {
    position: absolute;
    width: 100%;
    height: 100%;
    background-image: url('data:image/svg+xml,<svg viewBox="0 0 1000 1000" xmlns="http://www.w3.org/2000/svg"><circle cx="50" cy="50" r="10" fill="%23FFD70022"/><circle cx="90%" cy="30%" r="8" fill="%23FFD70022"/><circle cx="85%" cy="80%" r="12" fill="%23FFD70022"/></svg>');
    animation: particles 20s linear infinite;
}

@@keyframes particles {
    0% { background-position: 0 0; }
    100% { background-position: 1000px 1000px; }
}

/* Text Content */
.le-text-content {
    position: relative;
    z-index: 3;
}

.le-title-group {
    margin-bottom: 2.5rem;
}

.le-main-title {
    font-family: 'Playfair Display', serif;
    font-size: 4rem;
    line-height: 1.1;
    color: var(--pure-white);
    margin-bottom: 1rem;
}

.le-gradient-text {
    background: var(--gradient-accent);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
}

.le-title-underline {
    width: 120px;
    height: 4px;
    background: var(--gradient-accent);
    border-radius: 2px;
    margin-top: 1.5rem;
}

.le-subtitle {
    font-size: 1.5rem;
    color: rgba(255, 255, 255, 0.9);
    margin-bottom: 3rem;
    font-weight: 300;
    max-width: 600px;
}

/* Welcome Card */
.le-welcome-card {
    background: rgba(255, 255, 255, 0.1);
    backdrop-filter: blur(10px);
    border-radius: 1rem;
    padding: 1.5rem;
    margin-bottom: 3rem;
    position: relative;
    overflow: hidden;
    border: 1px solid rgba(255, 215, 0, 0.3);
    transition: transform 0.3s ease;
}

.le-welcome-card:hover {
    transform: translateY(-5px);
}

.le-welcome-card i {
    font-size: 2rem;
    color: var(--accent-gold);
    margin-bottom: 1rem;
}

.le-welcome-text {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
}

.le-welcome-title {
    font-size: 1.5rem;
    font-weight: 600;
    color: var(--pure-white);
}

.le-welcome-sub {
    font-size: 1rem;
    color: rgba(255, 255, 255, 0.8);
}

.le-underline-animation {
    position: absolute;
    bottom: 0;
    left: 0;
    width: 100%;
    height: 3px;
    background: var(--gradient-accent);
    transform: scaleX(0);
    transition: transform 0.3s ease;
}

.le-welcome-card:hover .le-underline-animation {
    transform: scaleX(1);
}

/* Stats Grid */
.le-stats-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2rem;
    margin-bottom: 3rem;
}

.le-stat-card {
    background: rgba(255, 255, 255, 0.05);
    padding: 1.5rem;
    border-radius: 0.5rem;
    text-align: center;
    transition: transform 0.3s ease;
}

.le-stat-card:hover {
    transform: translateY(-5px);
}

.le-stat-number {
    font-size: 2.5rem;
    font-weight: 700;
    color: var(--accent-gold);
    margin-bottom: 0.5rem;
}

.le-stat-label {
    font-size: 1rem;
    color: rgba(255, 255, 255, 0.8);
    text-transform: uppercase;
    letter-spacing: 1px;
}

/* CTA Buttons */
.le-cta-group {
    display: flex;
    gap: 1.5rem;
}

.le-main-cta, .le-secondary-cta {
    position: relative;
    padding: 1.2rem 2rem;
    border-radius: 0.5rem;
    display: inline-flex;
    align-items: center;
    gap: 0.8rem;
    text-decoration: none;
    overflow: hidden;
    transition: all 0.3s ease;
}

.le-main-cta {
    background: var(--gradient-accent);
    color: var(--dark-blue);
    font-weight: 600;
}

.le-secondary-cta {
    border: 2px solid rgba(255, 255, 255, 0.3);
    color: var(--pure-white);
}

.le-cta-hover {
    position: absolute;
    background: rgba(255, 255, 255, 0.1);
    width: 100%;
    height: 100%;
    left: -100%;
    transition: left 0.3s ease;
}

.le-main-cta:hover .le-cta-hover,
.le-secondary-cta:hover .le-cta-hover {
    left: 0;
}

/* Image Section */
.le-visual-content {
    position: relative;
    perspective: 1000px;
}

.le-image-container {
    border-radius: 1.5rem;
    overflow: hidden;
    transform: rotate3d(0.5, -0.5, 0, 15deg);
    box-shadow: 0 30px 50px rgba(0,0,0,0.3);
}

.le-main-image {
    width: 100%;
    height: auto;
    display: block;
}

.le-live-badge {
    position: absolute;
    bottom: -1rem;
    left: -1rem;
    background: rgba(255, 215, 0, 0.9);
    color: var(--dark-blue);
    padding: 0.8rem 1.5rem;
    border-radius: 2rem;
    display: flex;
    align-items: center;
    gap: 0.8rem;
    font-weight: 600;
    animation: pulse 2s infinite;
}

.le-pulse-dot {
    width: 10px;
    height: 10px;
    background: #FF4444;
    border-radius: 50%;
}

@@keyframes pulse {
    0% { transform: scale(1); }
    50% { transform: scale(1.05); }
    100% { transform: scale(1); }
}

/* Responsive Design */
@@media (max-width: 1200px) {
    .le-hero-content {
        grid-template-columns: 1fr;
        text-align: center;
    }

    .le-title-underline {
        margin: 1.5rem auto;
    }

    .le-stats-grid {
        grid-template-columns: 1fr;
        max-width: 500px;
        margin: 0 auto 3rem;
    }

    .le-cta-group {
        justify-content: center;
    }

    .le-image-container {
        max-width: 600px;
        margin: 2rem auto 0;
    }
}

@@media (max-width: 768px) {
    .le-main-title {
        font-size: 2.5rem;
    }

    .le-subtitle {
        font-size: 1.2rem;
    }

    .le-stat-number {
        font-size: 2rem;
    }

    .le-main-cta, .le-secondary-cta {
        padding: 1rem 1.5rem;
        font-size: 0.9rem;
    }
}
</style>

<script>
document.addEventListener('DOMContentLoaded', function() {
    // Animated number counters
    const counters = document.querySelector All('.le-stat-number');
    counters.forEach(counter => {
        const countTo = parseInt(counter.getAttribute('data-count'));
        let count = 0;
        const increment = Math.ceil(countTo / 100);
        const updateCount = setInterval(() => {
            count += increment;
            if (count >= countTo) {
                count = countTo;
                clearInterval(updateCount);
            }
            counter.textContent = count;
        }, 20);
    });
});
</script> 


<link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css" />
<script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"></script>

<div class="le-hero-section">
    <div class="le-hero-content">
        <h1 class="le-hero-title">Schedule Academic Session</h1>
        <p class="le-hero-subtitle">Personalized learning with expert educators</p>
    </div>
</div>

<div class="le-order-container">
    <div class="le-order-card">
        <form asp-action="CreateOrder" method="post" class="le-order-form">
            <div class="le-form-grid">
                <!-- Teacher Selection -->
                <div class="le-form-group">
                    <label class="le-form-label" for="TeacherId">Select Educator</label>
                    <select class="le-form-input" id="TeacherId" name="TeacherId" required>
                        <option value="">Choose a teacher...</option>
                        @foreach (var teacher in Model.Item2.Where(t => t.UserRole == "teacher"))
                        {
                            <option value="@teacher.UserId">Prof. @teacher.UserName - @teacher.TeacherSubject</option>
                        }
                    </select>
                    <i class="bi bi-person-badge le-form-icon"></i>
                </div>

                <!-- Subject Selection -->
                <div class="le-form-group">
                    <label class="le-form-label" for="SubjectId">Subject</label>
                    <select class="le-form-input" id="SubjectId" name="SubjectId" required>
                        <option value="">Select subject...</option>
                        @foreach (var subject in Model.Item1)
                        {
                            <option value="@subject.SubjectId">@subject.SubjectName</option>
                        }
                    </select>
                    <i class="bi bi-journal-bookmark le-form-icon"></i>
                </div>

                <!-- Education Level -->
                <div class="le-form-group">
                    <label class="le-form-label" for="LevelId">Academic Level</label>
                    <select class="le-form-input" id="LevelId" name="LevelId" required>
                        <option value="">Select your level</option>
                        @foreach (var level in Model.Item4)
                        {
                            <option value="@level.LevelId">@level.LevelName</option>
                        }
                    </select>
                    <i class="bi bi-mortarboard le-form-icon"></i>
                </div>

                <!-- Date & Time -->
                <div class="le-form-group">
                    <label class="le-form-label" for="RequestedDate">Preferred Date</label>
                    <input type="date" class="le-form-input" id="RequestedDate" name="RequestedDate" 
                           min="@DateTime.Now.ToString("yyyy-MM-dd")" required>
                    <i class="bi bi-calendar-check le-form-icon"></i>
                </div>

                <div class="le-form-group">
                    <label class="le-form-label" for="RequestedTime">Preferred Time</label>
                    <input type="time" class="le-form-input" id="RequestedTime" name="RequestedTime" 
                           min="08:00" max="20:00" required>
                    <i class="bi bi-clock-history le-form-icon"></i>
                </div>

                <!-- Location Picker -->
                <div class="le-form-group">
                    <label class="le-form-label">Session Location</label>
                    <div class="le-location-wrapper">
                        <input type="text" class="le-form-input" id="Location" name="Location" 
                               value="@userLocation" placeholder="Click to set location" required readonly>
                        <button type="button" class="le-map-trigger" onclick="showMap()">
                            <i class="bi bi-geo-alt"></i> Set Location
                        </button>
                    </div>
                    <span class="text-danger" id="location-error"></span>
                </div>

                <!-- Additional Notes -->
                <div class="le-form-group">
                    <label class="le-form-label" for="AdditionalNotes">Learning Objectives</label>
                    <textarea class="le-form-input" id="AdditionalNotes" name="AdditionalNotes" 
                              rows="3" placeholder="Describe your learning goals"></textarea>
                    <i class="bi bi-pencil le-form-icon"></i>
                </div>
            </div>

            <div class="le-form-submit">
                <button type="submit" class="le-academic-btn">
                    <i class="bi bi-journal-check"></i> Confirm Booking
                </button>
            </div>
        </form>
    </div>
</div>

<!-- Map Modal -->
<div id="mapModal" class="le-modal">
    <div class="le-modal-content">
        <div class="le-modal-header">
            <h3 class="le-modal-title"><i class="bi bi-map"></i> Select Session Location</h3>
            <span class="le-modal-close" onclick="closeMap()">&times;</span>
        </div>
        <div id="map" class="le-modal-map"></div>
        <div class="le-modal-footer">
            <button type="button" class="le-academic-btn" onclick="closeMap()">
                <i class="bi bi-check-circle"></i> Confirm Location
            </button>
        </div>
    </div>
</div>

<script>
    let map, marker;

    function showMap() {
        document.getElementById('mapModal').style.display = 'block';
        initMap();
    }

    function closeMap() {
        document.getElementById('mapModal').style.display = 'none';
    }

    function initMap() {
        if (!map) {
            map = L.map('map').setView([33.5731, -7.5898], 13);
            L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
                attribution: '&copy; OpenStreetMap contributors'
            }).addTo(map);

            map.on('click', async function(e) {
                try {
                    const response = await fetch(`https://nominatim.openstreetmap.org/reverse?lat=${e.latlng.lat}&lon=${e.latlng.lng}&format=json`);
                    const data = await response.json();
                    
                    document.getElementById('Location').value = data.display_name;
                    document.getElementById('location-error').textContent = '';
                    
                    if (marker) map.removeLayer(marker);
                    marker = L.marker(e.latlng).addTo(map)
                        .bindPopup(`<b>Selected Location:</b><br>${data.display_name}`);
                } catch (error) {
                    console.error('Geocoding error:', error);
                    document.getElementById('location-error').textContent = 
                        'Error determining address. Please try another location.';
                }
            });

            if (navigator.geolocation) {
                navigator.geolocation.getCurrentPosition(position => {
                    map.setView([position.coords.latitude, position.coords.longitude], 15);
                });
            }
        }
    }

    window.onclick = function(event) {
        const modal = document.getElementById('mapModal');
        if (event.target == modal) {
            closeMap();
        }
    }

    document.querySelector('.le-order-form').addEventListener('submit', function(e) {
        if (!document.getElementById('Location').value) {
            e.preventDefault();
            document.getElementById('location-error').textContent = 'Please select a location';
        }
    });
</script>

<style>
    .le-order-container {
        padding: 4rem 5%;
        background: var(--light-blue);
    }

    .le-order-card {
        background: white;
        border-radius: 1rem;
        padding: 2rem;
        box-shadow: 0 4px 6px rgba(0,0,0,0.05);
        max-width: 1000px;
        margin: 0 auto;
    }

    .le-form-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        gap: 1.5rem;
    }

    .le-form-group {
        position: relative;
        margin-bottom: 1rem;
    }

    .le-form-label {
        display: block;
        color: var(--dark-blue);
        font-weight: 500;
        margin-bottom: 0.5rem;
    }

    .le-form-input {
        width: 100%;
        padding: 0.875rem 1.25rem;
        border: 2px solid var(--light-blue);
        border-radius: 0.5rem;
        font-size: 1rem;
        transition: all 0.3s ease;
        background: white;
    }

    .le-form-input:focus {
        border-color: var(--accent-gold);
        box-shadow: 0 0 0 3px rgba(255,215,0,0.2);
        outline: none;
    }

    .le-form-icon {
        position: absolute;
        right: 1rem;
        top: 50%;
        transform: translateY(-50%);
        color: var(--primary-blue);
    }

    .le-location-wrapper {
        position: relative;
    }

    .le-map-trigger {
        position: absolute;
        right: 0.5rem;
        top: 50%;
        transform: translateY(-50%);
        background: var(--accent-gold);
        color: var(--dark-blue);
        border: none;
        padding: 0.5rem 1rem;
        border-radius: 0.5rem;
        cursor: pointer;
        display: flex;
        align-items: center;
        gap: 0.5rem;
        transition: all 0.3s ease;
    }

    .le-map-trigger:hover {
        background: #ffcc00;
    }

    .le-modal {
        display: none;
        position: fixed;
        z-index: 1000;
        left: 0;
        top: 0;
        width: 100%;
        height: 100%;
        background-color: rgba(0,0,0,0.5);
    }

    .le-modal-content {
        background-color: white;
        margin: 2% auto;
        padding: 1.5rem;
        border-radius: 1rem;
        width: 90%;
        max-width: 800px;
        position: relative;
    }

    .le-modal-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 1rem;
    }

    .le-modal-title {
        color: var(--dark-blue);
        font-family: 'Playfair Display', serif;
        margin: 0;
        display: flex;
        align-items: center;
        gap: 0.5rem;
    }

    .le-modal-close {
        color: var(--dark-blue);
        font-size: 2rem;
        cursor: pointer;
        transition: color 0.3s ease;
    }

    .le-modal-close:hover {
        color: var(--primary-blue);
    }

    .le-modal-map {
        height: 60vh;
        border-radius: 0.5rem;
        overflow: hidden;
        border: 2 ```html
        px solid var(--light-blue);
    }

    .le-modal-footer {
        margin-top: 1rem;
        text-align: right;
    }

    .le-academic-btn {
        background: var(--accent-gold);
        color: var(--dark-blue);
        border: none;
        padding: 0.75rem 1.5rem;
        border-radius: 0.5rem;
        cursor: pointer;
        transition: background 0.3s ease;
    }

    .le-academic-btn:hover {
        background: #ffcc00;
    }

    @@media (max-width: 768px) {
        .le-modal-content {
            width: 95%;
            margin: 5% auto;
        }

        .le-modal-map {
            height: 50vh;
        }
    }
</style>


<div class="le-services-container">
    <!-- ... keep existing services grid content ... -->
</div>

<!-- Rest of the existing home page content remains unchanged -->


<style>
    .le-services-container {
        padding: 4rem 5%;
        background: var(--light-blue);
    }

    .le-services-header {
        text-align: center;
        margin-bottom: 3rem;
    }

    .le-services-grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
        gap: 2rem;
        max-width: 1200px;
        margin: 0 auto;
    }

    .le-service-card {
        background: white;
        border-radius: 0.75rem;
        padding: 2rem;
        text-align: center;
        transition: transform 0.3s ease;
        box-shadow: 0 4px 6px rgba(0,0,0,0.05);
        border: 1px solid var(--light-blue);
        min-height: 300px;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
    }

        .le-service-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 20px rgba(42,92,130,0.1);
        }

    .le-service-icon {
        width: 80px;
        height: 80px;
        object-fit: contain;
        margin-bottom: 1.5rem;
        padding: 1rem;
        background: var(--light-blue);
        border-radius: 50%;
    }

    .le-service-title {
        color: var(--dark-blue);
        font-family: 'Playfair Display', serif;
        font-size: 1.25rem;
        margin-bottom: 1rem;
    }

    .le-service-description {
        color: var(--text-dark);
        line-height: 1.6;
        font-size: 0.95rem;
    }

    @@media (max-width: 768px) {
        .le-services-grid {
            grid-template-columns: 1fr;
            padding: 0 1rem;
        }

        .le-service-card {
            min-height: 250px;
            padding: 1.5rem;
        }
    }
</style>

<div class="le-hero-section">
    <div class="le-hero-content">
        <h1 class="le-hero-title">Academic Services</h1>
        <p class="le-hero-subtitle">Comprehensive Learning Solutions for Academic Success</p>
    </div>
</div>

<div class="le-services-container">
    <div class="le-services-grid">
        <a href="subjects/computer_courses.html" class="le-service-link">
            <div class="le-service-card">
                <img class="le-service-icon" src="~/images/computer-courses.png" alt="Computer Courses">
                <h3 class="le-service-title">Digital Literacy Programs</h3>
                <p class="le-service-description">Comprehensive computer science curriculum with certification programs</p>
            </div>
        </a>

        <a href="subjects/jee.html" class="le-service-link">
            <div class="le-service-card">
                <img class="le-service-icon" src="~/images/brainbooster.png" alt="Exam Preparation">
                <h3 class="le-service-title">Exam Preparation</h3>
                <p class="le-service-description">Structured programs for competitive exams and university admissions</p>
            </div>
        </a>

        <a href="#" class="le-service-link">
            <div class="le-service-card">
                <img class="le-service-icon" src="~/images/online-tutorials.png" alt="Lectures">
                <h3 class="le-service-title">Lecture Series</h3>
                <p class="le-service-description">Recorded and live lectures from top academic professionals</p>
            </div>
        </a>

        <a href="subjects/jee.html#sample_papers" class="le-service-link">
            <div class="le-service-card">
                <img class="le-service-icon" src="~/images/papers.jpg" alt="Resources">
                <h3 class="le-service-title">Academic Resources</h3>
                <p class="le-service-description">Curated collection of sample papers and study materials</p>
            </div>
        </a>

        <a href="#" class="le-service-link">
            <div class="le-service-card">
                <img class="le-service-icon" src="~/images/p3.png" alt="Analytics">
                <h3 class="le-service-title">Performance Analytics</h3>
                <p class="le-service-description">Detailed progress tracking and personalized feedback reports</p>
            </div>
        </a>

        <a href="#contactus_section" class="le-service-link">
            <div class="le-service-card">
                <img class="le-service-icon" src="~/images/discussion.png" alt="Mentorship">
                <h3 class="le-service-title">Academic Mentorship</h3>
                <p class="le-service-description">One-on-one sessions with subject matter experts</p>
            </div>
        </a>

        <a href="subjects/quiz.html" class="le-service-link">
            <div class="le-service-card">
                <img class="le-service-icon" src="~/images/q1.png" alt="Learning Tools">
                <h3 class="le-service-title">Learning Enhancers</h3>
                <p class="le-service-description">Interactive quizzes and critical thinking exercises</p>
            </div>
        </a>

        <a href="#contactus_section" class="le-service-link">
            <div class="le-service-card">
                <img class="le-service-icon" src="~/images/help.png" alt="Support">
                <h3 class="le-service-title">Academic Support</h3>
                <p class="le-service-description">24/7 access to our student success team</p>
            </div>
        </a>
    </div>
</div>


<div class="le-faculty-container">
    <div class="le-hero-section">
        <div class="le-hero-content">
            <h1 class="le-hero-title">Academic Faculty</h1>
            <p class="le-hero-subtitle">Meet Our Distinguished Educators</p>
        </div>
    </div>

    <div class="le-faculty-grid">
        @foreach (var teacher in Model.Item2.Where(u => u.UserRole == "teacher"))
        {
            <div class="le-teacher-card">
                <div class="le-card-content">
                    <div class="le-avatar-wrapper">
                        <div class="le-avatar-container">
                            @if (!string.IsNullOrEmpty(teacher.UserImage))
                            {
                                <img src="~/img/@teacher.UserImage" alt="@teacher.UserName" class="le-avatar">
                            }
                            else
                            {
                                <div class="le-avatar-fallback">
                                    <i class="bi bi-person-badge"></i>
                                </div>
                            }
                            <div class="le-experience-badge">
                                <i class="bi bi-mortarboard"></i>
                                @teacher.Experience+ Years
                            </div>
                        </div>
                    </div>
                    <div class="le-teacher-info">
                        <h3 class="le-teacher-name">Tchr. @teacher.UserName</h3>
                        <div class="le-teacher-department">@teacher.TeacherSubject</div>
                        <div class="le-teacher-meta">
                            <div class="le-meta-item">
                                <i class="bi bi-building"></i>
                                @teacher.Address
                            </div>
                            <div class="le-meta-item">
                                <i class="bi bi-clock-history"></i>
                                @teacher.HourlyRate Contact Hours
                            </div>
                        </div>
                    </div>
                    @if (!string.IsNullOrEmpty(teacher.Bio))
                    {
                        <div class="le-teacher-bio">
                            <p>@teacher.Bio</p>
                        </div>
                    }
                    <div class="le-card-actions">
                        <a asp-action="TeacherProfile" asp-route-id="@teacher.UserId" class="le-profile-button">
                            <i class="bi bi-file-person"></i> View Profile
                        </a>
                    </div>
                </div>
            </div>
        }
    </div>
</div>

<style>
    :root {
        --primary-blue: #2A5B82;
        --dark-blue: #1A3A4D;
        --light-blue: #F5F9FC;
        --accent-gold: #FFD700;
        --text-dark: #333333;
    }

    .le-faculty-container {
        background: var(--light-blue);
        padding-bottom: 4rem;
        min-height: 100vh;
    }

    .le-hero-section {
        background: linear-gradient(135deg, var(--primary-blue) 0%, var(--dark-blue) 100%);
        padding: 6rem 2rem 4rem;
        margin-bottom: 3rem;
    }

    .le-hero-content {
        max-width: 1200px;
        margin: 0 auto;
        text-align: center;
    }

    .le-hero-title {
        color: white;
        font-size: 2.5rem;
        margin-bottom: 1rem;
        font-family: 'Playfair Display', serif;
    }

    .le-hero-subtitle {
        color: rgba(255,255,255,0.9);
        font-size: 1.2rem;
        font-weight: 300;
    }

    .le-faculty-grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
        gap: 2rem;
        padding: 0 5%;
        max-width: 1200px;
        margin: 0 auto;
    }

    .le-teacher-card {
        background: white;
        border-radius: 0.75rem;
        box-shadow: 0 4px 6px rgba(0,0,0,0.05);
        transition: transform 0.3s ease, box-shadow 0.3s ease;
        overflow: hidden;
        display: flex;
        flex-direction: column;
    }

    .le-card-content {
        display: flex;
        flex-direction: column;
        flex-grow: 1;
    }

    .le-avatar-wrapper {
        padding: 2rem;
        background: var(--light-blue);
        position: relative;
    }

    .le-avatar-container {
        position: relative;
        width: 160px;
        height: 160px;
        margin: 0 auto;
    }

    .le-avatar {
        width: 100%;
        height: 100%;
        border-radius: 50%;
        object-fit: cover;
        border: 3px solid var(--primary-blue);
        box-shadow: 0 4px 15px rgba(42,92,130,0.1);
    }

    .le-avatar-fallback {
        width: 100%;
        height: 100%;
        border-radius: 50%;
        background: var(--light-blue);
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 3rem;
        color: var(--primary-blue);
        border: 2px dashed var(--primary-blue);
    }

    .le-experience-badge {
        position: absolute;
        bottom: -10px;
        right: -10px;
        background: var(--accent-gold);
        color: var(--dark-blue);
        padding: 0.5rem 1rem;
        border-radius: 2rem;
        font-weight: 600;
        display: flex;
        align-items: center;
        gap: 0.5rem;
        font-size: 0.9rem;
        box-shadow: 0 2px 8px rgba(0,0,0,0.1);
    }

    .le-teacher-info {
        padding: 1.5rem 1.5rem 0;
        text-align: center;
    }

    .le-teacher-name {
        color: var(--dark-blue);
        font-family: 'Playfair Display', serif;
        font-size: 1.5rem;
        margin-bottom: 0.75rem;
    }

    .le-teacher-department {
        color: var(--primary-blue);
        font-weight: 500;
        margin-bottom: 1.5rem;
        position: relative;
        display: inline-block;
        padding-bottom: 0.5rem;
    }

        .le-teacher-department::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 50%;
            transform: translateX(-50%);
            width: 40px;
            height: 2px;
            background: var(--accent-gold);
        }

    .le-teacher-meta {
        display: flex;
        flex-direction: column;
        gap: 0.75rem;
        font-size: 0.95rem;
        color: var(--text-dark);
        margin: 1.5rem 0;
    }

    .le-meta-item {
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 0.75rem;
        padding: 0.5rem 0;
    }

    .le-teacher-bio {
        padding: 1rem 1.5rem;
        margin: 0 1.5rem 1rem;
        background: var(--light-blue);
        border-radius: 0.5rem;
        font-size: 0.95rem;
        line-height: 1.6;
        color: var(--text-dark);
        flex-grow: 1;
        max-height: 150px;
        overflow-y: auto;
        scrollbar-width: thin;
        scrollbar-color: var(--primary-blue) var(--light-blue);
    }

        .le-teacher-bio::-webkit-scrollbar {
            width: 6px;
        }

        .le-teacher-bio::-webkit-scrollbar-track {
            background: var(--light-blue);
            border-radius: 0 0 0.5rem 0.5rem;
        }

        .le-teacher-bio::-webkit-scrollbar-thumb {
            background-color: var(--primary-blue);
            border-radius: 6px;
        }

    .le-card-actions {
        padding: 1.5rem;
        text-align: center;
        border-top: 2px solid var(--light-blue);
        margin-top: auto;
    }

    .le-profile-button {
        background: var(--primary-blue);
        color: white;
        padding: 0.8rem 2rem;
        border: none;
        border-radius: 0.5rem;
        font-weight: 500;
        cursor: pointer;
        transition: all 0.3s ease;
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        width: calc(100% - 3rem);
        justify-content: center;
        margin: 0 1.5rem;
    }

        .le-profile-button:hover {
            background: var(--dark-blue);
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(42,92,130,0.2);
        }

    @@media (max-width: 768px) {
        .le-hero-section {
            padding: 4rem 1rem 2rem;
        }

        .le-hero-title {
            font-size: 2rem;
        }

        .le-faculty-grid {
            padding: 0 2%;
            gap: 1.5rem;
        }

        .le-avatar-container {
            width: 140px;
            height: 140px;
        }

        .le-teacher-bio {
            max-height: 120px;
            margin: 0 1rem 1rem;
            padding: 0.75rem 1rem;
        }

        .le-profile-button {
            width: calc(100% - 2rem);
            margin: 0 1rem;
            padding: 0.6rem 1rem;
        }
    }

    @@media (max-width: 480px) {
        .le-hero-title {
            font-size: 1.75rem;
        }

        .le-hero-subtitle {
            font-size: 1rem;
        }

        .le-faculty-grid {
            grid-template-columns: 1fr;
        }
    }
</style>

    
<div class="le-hero-section">
    <div class="le-hero-content">
        <h1 class="le-hero-title">Academic Programs</h1>
        <p class="le-hero-subtitle">Explore Our Comprehensive Curriculum</p>
    </div>
</div>

<div class="le-subjects-container">
    <div class="le-subjects-grid">
        @foreach (var item in Model.Item1)
        {
            <a href="#" class="le-subject-card">
                <div class="le-subject-image">
                    @if (!string.IsNullOrEmpty(item.SubjectImage))
                    {
                        <img src="@Url.Content("/img/"+ item.SubjectImage)" alt="@item.SubjectName">
                    }
                    else
                    {
                        <img src="~/img/History_Jordan.jpg" alt="Academic Subject">
                    }
                
                </div>
                <div class="le-subject-content">
                    <h3 class="le-subject-title">@item.SubjectName</h3>
                    <div class="le-subject-meta">
                        <span class="le-credit-hours">3 Credit Hours</span>
                        <span class="le-course-level">Undergraduate</span>
                    </div>
                </div>
            </a>
        }
    </div>
</div>

<style>
    .le-subjects-container {
        padding: 4rem 5%;
        background: var(--light-blue);
    }

    .le-subjects-grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
        gap: 2rem;
        max-width: 1200px;
        margin: 0 auto;
    }

    .le-subject-card {
        background: white;
        border-radius: 0.75rem;
        overflow: hidden;
        transition: transform 0.3s ease;
        box-shadow: 0 4px 6px rgba(0,0,0,0.05);
        text-decoration: none;
    }

        .le-subject-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 20px rgba(42,92,130,0.1);
        }

    .le-subject-image {
        height: 200px;
        overflow: hidden;
        position: relative;
    }

        .le-subject-image img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.3s ease;
        }

    .le-subject-card:hover .le-subject-image img {
        transform: scale(1.05);
    }

    .le-subject-overlay {
        position: absolute;
        bottom: 0;
        left: 0;
        right: 0;
        padding: 1rem;
        background: linear-gradient(transparent, rgba(0,0,0,0.7));
    }

    .le-subject-code {
        color: white;
        font-size: 0.9rem;
        font-weight: 500;
    }

    .le-subject-content {
        padding: 1.5rem;
    }

    .le-subject-title {
        color: var(--dark-blue);
        font-family: 'Playfair Display', serif;
        font-size: 1.3rem;
        margin-bottom: 0.5rem;
    }

    .le-subject-meta {
        display: flex;
        justify-content: space-between;
        align-items: center;
        font-size: 0.9rem;
    }

    .le-credit-hours {
        color: var(--primary-blue);
        font-weight: 500;
    }

    .le-course-level {
        color: var(--text-dark);
        background: var(--light-blue);
        padding: 0.3rem 0.7rem;
        border-radius: 0.25rem;
    }

    @@media (max-width: 768px) {
        .le-subjects-grid {
            grid-template-columns: 1fr;
        }

        .le-subject-image {
            height: 180px;
        }
    }
</style>

<div class="le-hero-section">
    <div class="le-hero-content">
        <h1 class="le-hero-title">Contact ScholarMind</h1>
        <p class="le-hero-subtitle">Get in touch with our academic team</p>
    </div>
</div>

<div class="le-contact-section">
    <div class="le-contact-container">
        <div class="le-contact-card">
            <!-- Contact Information -->
            <div class="le-contact-info">
                <div class="le-contact-info-item">
                    <div class="le-contact-icon">
                        <i class="bi bi-telephone"></i>
                    </div>
                    <div class="le-contact-content">
                        <h3>Academic Support</h3>
                        <p>+212 782-434-378</p>
                        <span>Mon-Fri, 9am-5pm (GMT+1)</span>
                    </div>
                </div>

                <div class="le-contact-info-item">
                    <div class="le-contact-icon">
                        <i class="bi bi-envelope"></i>
                    </div>
                    <div class="le-contact-content">
                        <h3>Email Address</h3>
                        <p>academics@scholarmind.edu</p>
                        <span>Admissions: admissions@scholarmind.edu</span>
                    </div>
                </div>

                <div class="le-contact-info-item">
                    <div class="le-contact-icon">
                        <i class="bi bi-geo-alt"></i>
                    </div>
                    <div class="le-contact-content">
                        <h3>Campus Location</h3>
                        <p>123 Academic Way<br>Education City, Morocco</p>
                        <a href="#" class="le-campus-map">View Campus Map →</a>
                    </div>
                </div>
            </div>

            <!-- Contact Form -->
            <form asp-action="Create_us" method="post" class="le-contact-form">
                <div class="le-form-row">
                    <div class="le-form-group">
                        <input type="text" id="FullName" name="FullName" placeholder="Full Name" required />
                        <i class="bi bi-person le-input-icon"></i>
                    </div>
                    <div class="le-form-group">
                        <input type="email" id="Email" name="Email" placeholder="Email Address" required />
                        <i class="bi bi-envelope le-input-icon"></i>
                    </div>
                </div>

                <div class="le-form-group">
                    <input type="text" id="Subject" name="Subject" placeholder="Subject" />
                    <i class="bi bi-tag le-input-icon"></i>
                </div>

                <div class="le-form-group">
                    <textarea id="Message" name="Message" placeholder="Your Message" required></textarea>
                    <i class="bi bi-pencil le-input-icon"></i>
                </div>

                <div class="le-form-submit">
                    <button type="submit" class="le-academic-btn">
                        Send Message
                        <i class="bi bi-send"></i>
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>

<style>
    .le-contact-section {
        padding: 4rem 5%;
        background: var(--light-blue);
    }

    .le-contact-container {
        max-width: 1200px;
        margin: 0 auto;
    }

    .le-contact-card {
        background: white;
        border-radius: 1rem;
        box-shadow: 0 10px 30px rgba(0,0,0,0.08);
        display: grid;
        grid-template-columns: 1fr 1.5fr;
        gap: 2rem;
        padding: 2rem;
    }

    .le-contact-info {
        background: var(--primary-blue);
        padding: 2rem;
        border-radius: 0.75rem;
        color: white;
    }

    .le-contact-info-item {
        padding: 1.5rem;
        margin-bottom: 1.5rem;
        background: rgba(255,255,255,0.1);
        border-radius: 0.5rem;
    }

    .le-contact-icon {
        font-size: 1.5rem;
        color: var(--accent-gold);
        margin-bottom: 0.5rem;
    }

    .le-contact-content h3 {
        font-family: 'Playfair Display', serif;
        margin-bottom: 0.5rem;
        color: var(--accent-gold);
    }

    .le-contact-content p {
        font-weight: 500;
        margin-bottom: 0.25rem;
    }

    .le-contact-content span {
        font-size: 0.9rem;
        color: rgba(255,255,255,0.8);
    }

    .le-campus-map {
        color: var(--accent-gold);
        text-decoration: none;
        font-weight: 500;
        display: inline-block;
        margin-top: 0.5rem;
    }

    .le-contact-form {
        padding: 1rem;
    }

    .le-form-group {
        position: relative;
        margin-bottom: 1.5rem;
    }

    .le-input-icon {
        position: absolute;
        right: 1rem;
        top: 50%;
        transform: translateY(-50%);
        color: var(--primary-blue);
    }

    .le-contact-form input,
    .le-contact-form textarea {
        width: 100%;
        padding: 0.8rem 1rem;
        border: 2px solid var(--light-blue);
        border-radius: 0.5rem;
        font-family: 'Poppins', sans-serif;
        transition: all 0.3s ease;
    }

        .le-contact-form input:focus,
        .le-contact-form textarea:focus {
            border-color: var(--primary-blue);
            box-shadow: 0 0 0 3px rgba(42,92,130,0.1);
            outline: none;
        }

    .le-contact-form textarea {
        height: 120px;
        resize: vertical;
    }

    .le-academic-btn {
        background: var(--gradient-blue);
        color: white;
        padding: 0.8rem 2rem;
        border: none;
        border-radius: 0.5rem;
        font-weight: 600;
        cursor: pointer;
        display: flex;
        align-items: center;
        gap: 0.5rem;
        transition: transform 0.3s ease;
    }

        .le-academic-btn:hover {
            transform: translateY(-2px);
        }

    @@media (max-width: 992px) {
        .le-contact-card {
            grid-template-columns: 1fr;
        }

        .le-contact-info {
            padding: 1.5rem;
        }
    }

    @@media (max-width: 576px) {
        .le-contact-card {
            padding: 1rem;
        }

        .le-form-row {
            flex-direction: column;
            gap: 0;
        }
    }
</style>

<script>
    document.querySelector('.le-contact-form').addEventListener('submit', function (e) {
        const btn = this.querySelector('.le-academic-btn');
        btn.innerHTML = 'Sending... <i class="bi bi-arrow-repeat animate-spin"></i>';
        btn.disabled = true;
    });
</script>

<div class="le-hero-section">
    <div class="le-hero-content">
        <h1 class="le-hero-title">About ScholarMind Academy</h1>
        <p class="le-hero-subtitle">Empowering Lifelong Learning Through Innovative Education</p>
    </div>
</div>

<div class="le-about-container">
    <div class="le-about-grid">
        <!-- Image Section -->
        <div class="le-about-image-section">
            <div class="le-about-image-container">
                <img src="~/images/e3.jpg" class="le-about-main-image" alt="Academic Community">
                <div class="le-about-badge">
                    <i class="bi bi-award"></i>
                    Accredited Learning Institution
                </div>
            </div>
        </div>

        <!-- Content Section -->
        <div class="le-about-content-section">
            <div class="le-about-card">
                <h3 class="le-about-card-title">
                    <i class="bi bi-building-columns"></i>
                    Our Institution
                </h3>
                <div class="le-about-card-content">
                    <p class="le-about-description">
                        ScholarMind Academy is a premier online learning platform committed to academic excellence.
                        We combine traditional educational values with cutting-edge technology to create transformative
                        learning experiences for students worldwide.
                    </p>
                </div>
            </div>

            <div class="le-about-card">
                <h3 class="le-about-card-title">
                    <i class="bi bi-lightbulb"></i>
                    Academic Innovation
                </h3>
                <div class="le-about-card-content">
                    <p class="le-about-description">
                        Our platform integrates advanced learning technologies including AI-driven personalization,
                        interactive simulations, and collaborative virtual classrooms. We maintain rigorous academic
                        standards while embracing pedagogical innovation.
                    </p>
                </div>
            </div>

            <div class="le-about-card">
                <h3 class="le-about-card-title">
                    <i class="bi bi-compass"></i>
                    Academic Mission
                </h3>
                <div class="le-about-info-grid">
                    <div class="le-about-info-item">
                        <span class="le-about-info-label">Founded:</span>
                        <span class="le-about-info-value">2023</span>
                    </div>
                    <div class="le-about-info-item">
                        <span class="le-about-info-label">Students:</span>
                        <span class="le-about-info-value">10,000+</span>
                    </div>
                    <div class="le-about-info-item">
                        <span class="le-about-info-label">Courses:</span>
                        <span class="le-about-info-value">200+</span>
                    </div>
                    <div class="le-about-info-item">
                        <span class="le-about-info-label">Faculty:</span>
                        <span class="le-about-info-value">150+ Experts</span>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    .le-about-container {
        padding: 4rem 5%;
        background: var(--light-blue);
    }

    .le-about-grid {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 3rem;
        max-width: 1200px;
        margin: 0 auto;
    }

    .le-about-image-container {
        border-radius: 1rem;
        overflow: hidden;
        box-shadow: 0 10px 20px rgba(0,0,0,0.1);
        position: relative;
        height: 600px;
    }

    .le-about-main-image {
        width: 100%;
        height: 100%;
        object-fit: cover;
    }

    .le-about-badge {
        position: absolute;
        bottom: 20px;
        left: 20px;
        background: var(--accent-gold);
        color: var(--dark-blue);
        padding: 0.8rem 1.5rem;
        border-radius: 0.5rem;
        font-weight: 600;
        display: flex;
        align-items: center;
        gap: 0.5rem;
    }

    .le-about-card {
        background: white;
        border-radius: 0.75rem;
        padding: 2rem;
        margin-bottom: 2rem;
        box-shadow: 0 4px 6px rgba(0,0,0,0.05);
        border-left: 4px solid var(--primary-blue);
    }

    .le-about-card-title {
        color: var(--dark-blue);
        font-family: 'Playfair Display', serif;
        font-size: 1.5rem;
        margin-bottom: 1.5rem;
        display: flex;
        align-items: center;
        gap: 0.75rem;
    }

        .le-about-card-title i {
            color: var(--accent-gold);
            font-size: 1.8rem;
        }

    .le-about-description {
        color: var(--text-dark);
        line-height: 1.8;
        margin-bottom: 1rem;
    }

    .le-about-info-grid {
        display: grid;
        grid-template-columns: repeat(2, 1fr);
        gap: 1.5rem;
    }

    .le-about-info-item {
        background: var(--light-blue);
        padding: 1.2rem;
        border-radius: 0.5rem;
        display: flex;
        justify-content: space-between;
        align-items: center;
    }

    .le-about-info-label {
        color: var(--primary-blue);
        font-weight: 500;
    }

    .le-about-info-value {
        color: var(--dark-blue);
        font-weight: 600;
    }

    @@media (max-width: 992px) {
        .le-about-grid {
            grid-template-columns: 1fr;
        }

        .le-about-image-container {
            height: 400px;
        }
    }

    @@media (max-width: 768px) {
        .le-about-info-grid {
            grid-template-columns: 1fr;
        }

        .le-about-card {
            padding: 1.5rem;
        }
    }
</style>